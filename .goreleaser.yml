version: 2

builds:
  - id: nuon-ext-cf-stack
    main: .
    binary: nuon-ext-cf-stack
    ldflags:
      - -s -w -X github.com/nuonco/nuon-ext-cf-stack/cmd.BuildVersion={{ .Version }}
    env:
      - CGO_ENABLED=0
    goos:
      - linux
      - darwin
    goarch:
      - amd64
      - arm64

archives:
  - id: nuon-ext-cf-stack
    builds:
      - nuon-ext-cf-stack
    format: tar.gz
    name_template: "nuon-ext-cf-stack-{{ .Os }}-{{ .Arch }}"

checksum:
  name_template: "checksums.txt"

changelog:
  sort: asc
